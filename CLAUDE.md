# Block Tanks（仮）— CC_Company ゲームスタジオ部門

## プロジェクト概要

Wii Play のミニゲーム「タンク！(Tanks!)」の**ゲームメカニクス（ルール・挙動）だけ**を仕様として再現した、ブラウザで動くトップダウン（真上視点）戦車対戦ゲーム。個人開発。

- **知財ポリシー（最重要）**: 任天堂の画像・音源・コード・固有名称は一切使わない。再現するのは著作権保護の対象外である「ルール＝メカニクス」のみ。ビジュアル・サウンド・名称はすべてオリジナル資産（自作またはCC0＝著作権放棄済みライセンスの素材）で構成する。
- **ゲーム名**: Block Tanks（仮）

## 確定済みの意思決定

| 項目 | 決定 | 備考 |
|---|---|---|
| プレイ形式 | シングルプレイ軸＋ローカル2P協力 | 2P協力はMVP完成後のフェーズで実装 |
| 操作方法 | WASD移動＋マウス照準・クリック射撃 | 2P側の操作はGDDの未決事項参照 |
| MVP規模 | 5ミッション・敵AI 2種 | 詳細は docs/gdd.md |
| 技術スタック | 素のHTML5 Canvas 1ファイルで試作 → 承認後 Phaser + Vite + TypeScript へ移行 | Phaser＝2Dゲーム用フレームワーク、Vite＝高速ビルドツール |

## 開発プロセス（フェーズゲート制）

**各フェーズの終わりに、必ずオーナー（ユーザー）の確認・承認を得てから次へ進む。承認前に次フェーズのコードを書かない。**

1. **Phase 0: 立ち上げ** — CLAUDE.md / エージェント定義 / docs/gdd.md の作成（本フェーズ）
2. **Phase 1: プロトタイプ** — Canvas 1ファイル（`prototype/index.html`）で核メカニクス（移動・照準・跳弾・敵1種）を検証
3. **Phase 2: 本実装移行** — Phaser + Vite + TypeScript へ移行、シーン管理・アセット導入
4. **Phase 3: MVP完成** — 5ミッション・敵2種・地雷・UI・効果音
5. **Phase 4: 拡張** — ローカル2P協力、敵種追加、ステージエディタ等（別途計画）

## リポジトリ構成（予定）

```
CLAUDE.md            … 本ファイル（プロジェクトの憲法）
.claude/agents/      … サブエージェント定義（6役職）
docs/gdd.md          … ゲームデザインドキュメント（仕様の一次ソース）
docs/decisions/      … 意思決定記録（ADR形式、必要になったら作成）
prototype/           … Phase 1 の Canvas 1ファイル試作
src/                 … Phase 2 以降の本実装（Phaser + TS）
assets/              … オリジナル画像・音源（出所とライセンスを assets/CREDITS.md に必ず記録）
```

## コーディング規約（Phase 2 以降）

- TypeScript は `strict: true`（厳格な型チェック）を有効にする。
- ゲームロジック（弾道計算・AI・当たり判定）は Phaser のシーンから分離した純粋な TS モジュールに置き、単体テスト（Vitest）可能にする。
- マジックナンバー（意味が読み取れない生の数値）は禁止。弾速・リロード時間などの調整値は `src/config/balance.ts` に集約する。
- 座標系・角度の単位（ラジアン/度）はプロジェクト全体で統一し、GDDに明記された値を正とする。

## サブエージェントの使い分け

`.claude/agents/` に6役職を定義済み。作業内容に応じて委譲する：

- **game-designer** — 仕様策定・GDD更新・メカニクスの原作分析
- **gameplay-programmer** — ゲームロジック・物理・AIの実装
- **level-designer** — ステージ設計・ミッション構成・難易度曲線
- **qa-debugger** — バグ調査・テスト・挙動検証
- **asset-advisor** — オリジナル資産の方針・ライセンス確認
- **producer** — 進行管理・フェーズゲート判定・スコープ管理

## 会話のルール

- 専門用語には必ず日本語で短い解説を添える（例：「スプライト＝ゲーム内の画像素材」）。
- 仕様の変更・追加はまず docs/gdd.md に反映してから実装する（GDDが常に一次ソース）。
- 破壊的な操作（ファイル削除・履歴書き換え・公開）は事前にオーナーへ確認する。
